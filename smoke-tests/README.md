# `smoke-tests`

This directory contains simple smoke tests to verify that the expected tooling has been installed correctly in the environment.
These tests run as part of the packer build process to ensure that the base image is functioning as expected.

This will typically be import tests or `cli --help` commands.
