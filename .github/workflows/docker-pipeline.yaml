name: Docker Build and Test

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read
  packages: write

env:
  AWS_REGION: us-east-1
  DOCKER_REPO: ghcr.io/omsf-eco-infra/omsf
  AWS_GHA_RUNNER_HOME_DIR: /home/ubuntu
  AWS_GHA_RUNNER_IMAGE_ID: ${{ vars.AWS_DOCKER_GHA_RUNNER_IMAGE_ID }}
  AWS_GHA_BUILD_RUNNER_INSTANCE_TYPE: ${{ vars.AWS_DOCKER_GHA_RUNNER_INSTANCE_TYPE }}
  # Match AMI test-stage instance type in ami-pipeline.yaml.
  AWS_GHA_TEST_RUNNER_INSTANCE_TYPE: g5.xlarge
  AWS_GHA_RUNNER_ROOT_DEVICE_SIZE: ${{ vars.AWS_DOCKER_GHA_RUNNER_ROOT_DEVICE_SIZE }}

jobs:
  start-aws-build-runner:
    name: Start AWS self-hosted build runner
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      mapping: ${{ steps.aws-start.outputs.mapping }}
      instances: ${{ steps.aws-start.outputs.instances }}
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DOCKER_GHA_RUNNER_ASSUME_ROLE_ARN }}
          role-session-name: gha-start-docker-build-runner
          aws-region: ${{ env.AWS_REGION }}

      - name: Validate runner configuration
        run: |
          set -euo pipefail
          : "${AWS_GHA_RUNNER_IMAGE_ID:?Repository variable AWS_DOCKER_GHA_RUNNER_IMAGE_ID is required}"
          : "${AWS_GHA_BUILD_RUNNER_INSTANCE_TYPE:?Repository variable AWS_DOCKER_GHA_RUNNER_INSTANCE_TYPE is required}"

      - name: Start runner
        id: aws-start
        uses: omsf/start-aws-gha-runner@v1.0.0
        with:
          aws_home_dir: ${{ env.AWS_GHA_RUNNER_HOME_DIR }}
          aws_image_id: ${{ env.AWS_GHA_RUNNER_IMAGE_ID }}
          aws_instance_type: ${{ env.AWS_GHA_BUILD_RUNNER_INSTANCE_TYPE }}
          aws_region_name: ${{ env.AWS_REGION }}
          aws_root_device_size: ${{ env.AWS_GHA_RUNNER_ROOT_DEVICE_SIZE }}
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

  build-docker:
    name: Build Docker image
    needs: start-aws-build-runner
    runs-on: ${{ fromJson(needs.start-aws-build-runner.outputs.instances) }}
    env:
      # Self-hosted runner jobs may not define HOME; setup-packer/packer requires it.
      HOME: /home/ubuntu
    outputs:
      image_repo: ${{ steps.collect.outputs.image_repo }}
      image_tag: ${{ steps.collect.outputs.image_tag }}
      environment_matrix: ${{ steps.collect.outputs.environment_matrix }}
    steps:
      - uses: actions/checkout@v6

      - name: Ensure runner dependencies (Ubuntu)
        run: |
          set -euo pipefail

          if command -v unzip >/dev/null 2>&1 && command -v docker >/dev/null 2>&1 && command -v python >/dev/null 2>&1; then
            exit 0
          fi

          sudo apt-get update
          sudo apt-get install -y unzip docker.io python3 python-is-python3

      - name: Set up Packer
        uses: hashicorp/setup-packer@v3
        with:
          packer_version: latest

      - name: Packer init
        run: packer init build-docker.pkr.hcl

      - name: Build Docker image
        env:
          DOCKER_REPO: ${{ env.DOCKER_REPO }}
        run: |
          set -euo pipefail
          packer build \
            -var-file "openfe.pkrvars.hcl" \
            -var "ami_name_suffix=ci-${{ github.run_id }}-${{ github.sha }}" \
            -var "additional_tags={}" \
            -var "docker_repository=${DOCKER_REPO}" \
            build-docker.pkr.hcl

      - name: Collect packer manifest outputs
        id: collect
        uses: ./.github/actions/collect-packer-metadata
        with:
          manifest_path: packer-docker-manifest.json
          kind: docker
          image_repo: ${{ env.DOCKER_REPO }}

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Push Docker image
        run: |
          set -euo pipefail
          docker push "${{ steps.collect.outputs.image_repo }}:${{ steps.collect.outputs.image_tag }}"

  stop-aws-build-runner:
    name: Stop AWS self-hosted build runner
    needs: [start-aws-build-runner, build-docker]
    if: ${{ always() && needs.start-aws-build-runner.outputs.mapping != '' }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DOCKER_GHA_RUNNER_ASSUME_ROLE_ARN }}
          role-session-name: gha-stop-docker-build-runner
          aws-region: ${{ env.AWS_REGION }}

      - name: Stop runner
        uses: omsf/stop-aws-gha-runner@v1.0.0
        with:
          instance_mapping: ${{ needs.start-aws-build-runner.outputs.mapping }}
          aws_region_name: ${{ env.AWS_REGION }}
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

  start-aws-test-runner:
    name: Start AWS self-hosted test runner
    needs: build-docker
    if: ${{ needs.build-docker.outputs.image_repo != '' }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      mapping: ${{ steps.aws-start.outputs.mapping }}
      instances: ${{ steps.aws-start.outputs.instances }}
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DOCKER_GHA_RUNNER_ASSUME_ROLE_ARN }}
          role-session-name: gha-start-docker-test-runner
          aws-region: ${{ env.AWS_REGION }}

      - name: Validate runner configuration
        run: |
          set -euo pipefail
          : "${AWS_GHA_RUNNER_IMAGE_ID:?Repository variable AWS_DOCKER_GHA_RUNNER_IMAGE_ID is required}"
          : "${AWS_GHA_TEST_RUNNER_INSTANCE_TYPE:?Runner instance type must be set}"

      - name: Start runner
        id: aws-start
        uses: omsf/start-aws-gha-runner@v1.0.0
        with:
          aws_home_dir: ${{ env.AWS_GHA_RUNNER_HOME_DIR }}
          aws_image_id: ${{ env.AWS_GHA_RUNNER_IMAGE_ID }}
          aws_instance_type: ${{ env.AWS_GHA_TEST_RUNNER_INSTANCE_TYPE }}
          aws_region_name: ${{ env.AWS_REGION }}
          aws_root_device_size: ${{ env.AWS_GHA_RUNNER_ROOT_DEVICE_SIZE }}
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

  test-environment:
    name: Test environment (${{ matrix.environment.name }})
    needs: [build-docker, start-aws-test-runner]
    if: ${{ needs.build-docker.outputs.image_repo != '' }}
    strategy:
      fail-fast: false
      matrix:
        environment: ${{ fromJson(needs.build-docker.outputs.environment_matrix) }}
    runs-on: ${{ fromJson(needs.start-aws-test-runner.outputs.instances) }}
    steps:
      - name: Ensure runner dependencies (Ubuntu)
        run: |
          set -euo pipefail

          if command -v docker >/dev/null 2>&1; then
            exit 0
          fi

          sudo apt-get update
          sudo apt-get install -y docker.io

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Pull image
        run: |
          set -euo pipefail
          docker pull "${{ needs.build-docker.outputs.image_repo }}:${{ needs.build-docker.outputs.image_tag }}"

      - name: Run full tests
        run: |
          set -euo pipefail
          env_name="${{ matrix.environment.name }}"
          docker run --rm \
            -e "MICROMAMBA_DEFAULT_ENVIRONMENT=${env_name}" \
            "${{ needs.build-docker.outputs.image_repo }}:${{ needs.build-docker.outputs.image_tag }}" \
            bash -lc "/tmp/environments/${env_name}/full-tests.sh"

  stop-aws-test-runner:
    name: Stop AWS self-hosted test runner
    needs: [start-aws-test-runner, test-environment]
    if: ${{ always() && needs.start-aws-test-runner.outputs.mapping != '' }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DOCKER_GHA_RUNNER_ASSUME_ROLE_ARN }}
          role-session-name: gha-stop-docker-test-runner
          aws-region: ${{ env.AWS_REGION }}

      - name: Stop runner
        uses: omsf/stop-aws-gha-runner@v1.0.0
        with:
          instance_mapping: ${{ needs.start-aws-test-runner.outputs.mapping }}
          aws_region_name: ${{ env.AWS_REGION }}
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
